# 新增/修改功能需求

## 一、基本信息

- **功能名称**：[请填写功能的中文名称]
- **功能类型**：[ ] 新增功能 [ ] 修改现有功能
- **优先级**：[ ] 高 [ ] 中 [ ] 低
- **预期完成时间**：[请填写预期完成日期]

---

## 二、功能背景与目标

### 2.1 业务背景
[请描述此功能的业务背景，为什么需要这个功能]

### 2.2 功能目标
[请描述此功能要实现的目标，解决什么问题]

### 2.3 用户角色
[请说明哪些角色会使用此功能]
- [ ] 总管理员
- [ ] 教师

**权限说明**：
- 总管理员和教师都拥有所有功能的访问权限
- 教师与教师之间在具体业务逻辑上可能有区别（如：数据范围、操作对象等），但功能权限相同
- 所有业务逻辑保持不变，仅移除权限控制相关的限制

---

## 三、功能详细描述

### 3.1 功能概述
[请用1-2句话简要描述功能]

### 3.2 功能流程
[请描述用户使用此功能的完整流程，建议使用步骤列表]

例如：
1. 用户点击"XX"按钮
2. 弹出XX对话框
3. 用户填写/选择XX信息
4. 点击确认后执行XX操作
5. 显示XX结果

### 3.3 功能场景
[请描述主要的使用场景]

**场景一**：[场景描述]
- 前置条件：[什么情况下可以使用]
- 操作步骤：[用户如何操作]
- 预期结果：[应该得到什么结果]

**场景二**：[如有其他场景，继续描述]

---

## 四、界面设计要求

### 4.1 页面/组件位置
- **页面路径**：[在哪个页面使用，如：`src/views/account/index.vue`]
- **菜单位置**：[在哪个菜单下，是否需要新增菜单项]
- **组件类型**：[ ] 独立页面 [ ] 对话框/模态框 [ ] 表单组件 [ ] 列表组件 [ ] 其他

### 4.2 UI设计参考
[如有设计稿，请提供]
- 设计稿链接/文件：[ ]
- 参考现有页面：[ ] 有，请说明参考哪个页面
- 完全自主设计：[ ]

### 4.3 界面元素说明
[请列出界面需要的元素]

**表单字段**（如有）：
| 字段名称 | 字段类型 | 是否必填 | 默认值 | 说明/校验规则 |
|---------|---------|---------|--------|--------------|
| 字段1 | 文本/数字/选择/日期等 | 是/否 | - | - |
| 字段2 | - | - | - | - |

**按钮**：
- [ ] 新增按钮
- [ ] 编辑按钮
- [ ] 删除按钮
- [ ] 查询/搜索按钮
- [ ] 重置按钮
- [ ] 提交/保存按钮
- [ ] 取消按钮
- [ ] 导出按钮
- [ ] 导入按钮
- [ ] 其他：[请说明]

**列表/表格**（如有）：
- 显示字段：[请列出需要显示的列]
- 是否支持排序：[ ] 是 [ ] 否
- 是否支持筛选：[ ] 是 [ ] 否
- 是否支持分页：[ ] 是 [ ] 否（默认每页显示：____ 条）

### 4.4 交互说明
- **操作反馈**：
  - 成功提示：[请说明成功时的提示信息]
  - 失败提示：[请说明失败时的提示信息]
  - 确认对话框：[是否需要删除/重要操作的确认对话框]

- **加载状态**：
  - [ ] 需要显示加载动画
  - [ ] 需要禁用按钮防止重复提交

---

## 五、API接口信息

### 5.1 接口列表
[请列出所有需要的API接口]

**接口一**：[接口功能名称]
- **接口地址**：`POST/GET/PUT/DELETE /exam/api/xxx`
- **请求参数**：
  ```json
  {
    "param1": "类型 - 说明",
    "param2": "类型 - 说明"
  }
  ```
- **响应格式**：
  ```json
  {
    "code": 200,
    "msg": "success",
    "data": {
      // 数据结构说明
    }
  }
  ```
- **错误码**（如有特殊错误码）：
  - `code: 400` - 参数错误
  - `code: 401` - 未授权
  - 其他：[请说明]

**接口二**：[继续列出其他接口]

### 5.2 数据格式说明
[请详细说明数据结构]

**请求数据格式**：
```typescript
interface RequestData {
  // 字段定义
}
```

**响应数据格式**：
```typescript
interface ResponseData {
  // 字段定义
}
```

### 5.3 特殊说明
- [ ] 需要文件上传（请说明文件类型、大小限制）
- [ ] 需要文件下载（请说明文件格式）
- [ ] 需要分页（请说明分页参数格式：offset/limit 还是 page/pageSize）
- [ ] 其他：[请说明]

**注意**：本系统不进行权限校验，总管理员和教师都拥有所有功能的访问权限。

---

## 六、业务规则与校验

### 6.1 业务规则
[请列出相关的业务规则]

例如：
- 规则1：[描述]
- 规则2：[描述]

### 6.2 数据校验
[请列出需要的数据校验规则]

- **前端校验**：
  - 必填项校验
  - 格式校验（邮箱、手机号、身份证等）
  - 长度校验
  - 范围校验（数值范围）
  - 自定义校验：[请说明]

- **后端校验**：[如有后端特殊校验要求，请说明]

### 6.3 权限控制
**权限规则**：
- **总管理员**和**教师**都拥有所有功能的访问权限
- 所有角色都可以进行增删改操作
- 不进行权限校验，所有业务逻辑保持不变

**业务区别说明**（如适用）：
- 教师与教师之间在具体业务上可能有区别（如：数据范围、操作对象等）
- 这些区别通过业务逻辑实现，而非权限控制
- 例如：[请说明具体的业务区别，如：教师A只能操作自己创建的数据，教师B可以操作所有数据等]

---

## 七、修改现有功能说明（如适用）

### 7.1 修改内容
[如果是修改现有功能，请详细说明]
- **原功能**：[原功能是什么]
- **修改点**：[要修改哪些地方]
- **影响范围**：[修改会影响哪些其他功能]

### 7.2 兼容性要求
- [ ] 需要兼容旧数据
- [ ] 需要数据迁移
- [ ] 向后兼容API
- [ ] 其他：[请说明]

---

## 八、特殊情况与边界处理

### 8.1 异常情况
[请说明需要处理的异常情况]

- 网络错误：[如何处理]
- 数据为空：[如何显示]
- 其他异常：[请说明]

**注意**：本系统不进行权限校验，因此不存在"权限不足"的情况。

### 8.2 边界情况
- 数据量大时：[如何处理，是否需要分页、虚拟滚动等]
- 并发操作：[如何处理]
- 数据一致性：[如何保证]

---

## 九、测试要求

### 9.1 功能测试点
[请列出需要测试的主要功能点]

1. [测试点1]
2. [测试点2]
3. [测试点3]

### 9.2 测试数据
[如有特定的测试数据要求，请提供]
- 测试账号：[ ]
- 测试数据：[ ]

### 9.3 浏览器兼容性
- [ ] Chrome（最新版本）
- [ ] Firefox（最新版本）
- [ ] Safari（最新版本）
- [ ] Edge（最新版本）
- [ ] 其他：[请说明]

---

## 十、参考资料

### 10.1 相关文档
- [ ] 有相关的业务文档/需求文档
- [ ] 有相关的API文档
- [ ] 有相关的设计文档

### 10.2 参考代码
[如有参考的现有代码，请提供文件路径]
- 参考页面：`src/views/xxx/xxx.vue`
- 参考组件：`src/components/xxx.vue`
- 参考API：`src/api/xxx.js/ts`

### 10.3 其他说明
[如有其他需要说明的内容，请在此填写]

---

## 十一、开发优先级说明（选填）

### 11.1 MVP（最小可行产品）
[请说明功能的最小可用版本是什么，哪些功能可以后续迭代]

### 11.2 后续优化
[请说明后续可能需要的优化或扩展]

---

## 使用说明

**填写提示**：
1. 请根据实际情况填写各个部分，不需要的部分可以删除
2. 界面设计要求部分，如有设计稿请尽量提供
3. API接口信息尽量详细，包括请求参数和响应格式
4. 业务规则要尽量全面，避免开发过程中的返工
5. 如有疑问或不明确的地方，请标注出来

**模板版本**：v1.0  
**最后更新**：2024年

